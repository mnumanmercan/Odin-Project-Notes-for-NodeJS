<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Application</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
</head>

<body class="bg-light">
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-lg-8 col-md-10">
                <div class="card shadow-lg border-0 rounded-4">
                    <div class="card-header bg-secondary text-white py-4 rounded-top-4">
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <i class="fas fa-folder-open fs-3 me-2"></i>
                                <span class="h2 mb-0 fw-bold"><%= title %></span>
                            </div>
                            <a href="/new-category" class="btn btn-light btn-lg rounded-3">
                                <i class="fas fa-plus-circle me-2"></i>
                                Yeni Kategori Ekle
                            </a>
                        </div>
                    </div>
                    
                    <div class="card-body p-0">
                        <div class="table-responsive">
                            <table class="table table-hover mb-0">
                                <thead class="table-light">
                                    <tr>
                                        <th scope="col" class="py-3 px-4" style="width: 100px;">
                                            <i class="fas fa-hashtag me-2 text-secondary"></i>ID
                                        </th>
                                        <th scope="col" class="py-3">
                                            <i class="fas fa-tag me-2 text-secondary"></i>Kategori Adı
                                        </th>
                                        <th scope="col" class="py-3 text-center" style="width: 150px;">
                                            <i class="fas fa-cog me-2 text-secondary"></i>İşlemler
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% categories.forEach(category=> { %>
                                        <tr class="align-middle">
                                            <th scope="row" class="px-4 fw-bold text-secondary">
                                                <%= category.id %>
                                            </th>
                                            <td class="fw-semibold">
                                                <span class="badge bg-secondary bg-opacity-10 text-dark fs-6 px-3 py-2">
                                                    <i class="fas fa-folder me-2"></i><%= category.name %>
                                                </span>
                                            </td>
                                            <td class="text-center">
                                                <a href="/delete-categories/<%= category.id %>" class="btn btn-danger btn-sm rounded-3">
                                                    <i class="fas fa-trash-alt me-1"></i>Sil
                                                </a>
                                            </td>
                                        </tr>
                                    <% }) %>
                                </tbody>
                            </table>
                        </div>
                        
                        <% if (categories.length === 0) { %>
                            <div class="text-center py-5">
                                <i class="fas fa-folder-open fs-1 text-muted mb-3"></i>
                                <p class="text-muted fs-5">Henüz kategori eklenmemiş</p>
                                <a href="/new-category" class="btn btn-secondary">
                                    <i class="fas fa-plus me-2"></i>İlk Kategoriyi Ekle
                                </a>
                            </div>
                        <% } %>
                    </div>
                    
                    <div class="card-footer bg-white border-0 py-3 rounded-bottom-4">
                        <div class="d-flex justify-content-between align-items-center px-3">
                            <span class="text-muted">
                                <i class="fas fa-folder me-2"></i>
                                Toplam <strong><%= categories.length %></strong> kategori
                            </span>
                            <a href="/" class="btn btn-secondary rounded-3">
                                <i class="fas fa-home me-2"></i>Ana Sayfaya Dön
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
        crossorigin="anonymous"></script>
</body>

</html>