<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Posts - Clubhouse</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-purple-50 to-pink-100 min-h-screen">
    
    <div class="min-h-screen py-8 px-4">
        <div class="max-w-5xl mx-auto">
            
            <!-- Header -->
            <div class="mb-8">
                <div class="flex items-center justify-between mb-4">
                    <a href="/" class="inline-flex items-center gap-2 text-gray-600 hover:text-gray-800 transition">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                        </svg>
                        Back to Home
                    </a>
                    
                    <% if (locals.user) { %>
                        <div class="flex items-center gap-3">
                            <span class="text-gray-600">
                                Welcome, <span class="font-semibold text-purple-600">@<%= user.username %></span>
                            </span>
                            <% if (user.role === 'admin') { %>
                                <span class="bg-red-500 text-white text-xs px-2 py-1 rounded-full">ADMIN</span>
                            <% } %>
                        </div>
                    <% } %>
                </div>

                <h1 class="text-5xl font-bold text-gray-800 mb-2">
                    📚 All Posts
                </h1>
                <p class="text-gray-600 text-lg">
                    <%= posts.length %> <%= posts.length === 1 ? 'story' : 'stories' %> shared in the clubhouse
                </p>
            </div>

            <!-- Member Status Banner -->
            <% if (locals.user) { %>
                <div class="bg-gradient-to-r from-green-400 to-emerald-500 rounded-xl p-4 mb-6 text-white shadow-lg">
                    <div class="flex items-center gap-3">
                        <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                        </svg>
                        <div>
                            <p class="font-semibold">You're a Clubhouse Member!</p>
                            <p class="text-sm text-green-100">You can see all authors and create posts.</p>
                        </div>
                    </div>
                </div>
            <% } else { %>
                <div class="bg-gradient-to-r from-yellow-400 to-orange-500 rounded-xl p-4 mb-6 text-white shadow-lg">
                    <div class="flex items-center gap-3">
                        <svg class="w-6 h-6" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"></path>
                        </svg>
                        <div class="flex-1">
                            <p class="font-semibold">Browsing as Guest</p>
                            <p class="text-sm text-yellow-100">Authors are hidden. <a href="/sign-up" class="underline font-semibold">Join us</a> to see who wrote what!</p>
                        </div>
                    </div>
                </div>
            <% } %>

            <!-- Posts Grid -->
            <% if (posts.length === 0) { %>
                <!-- Empty State -->
                <div class="bg-white rounded-2xl shadow-xl p-12 text-center">
                    <div class="w-20 h-20 bg-gray-100 rounded-full flex items-center justify-center mx-auto mb-4">
                        <svg class="w-10 h-10 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                        </svg>
                    </div>
                    <h3 class="text-2xl font-bold text-gray-800 mb-2">No Posts Yet</h3>
                    <p class="text-gray-600 mb-6">Be the first to share a story in the clubhouse!</p>
                    <% if (locals.user) { %>
                        <a href="/new-post" class="inline-block bg-gradient-to-r from-purple-500 to-pink-600 hover:from-purple-600 hover:to-pink-700 text-white font-bold py-3 px-6 rounded-xl transition duration-200 transform hover:scale-105 shadow-lg">
                            Create First Post
                        </a>
                    <% } else { %>
                        <a href="/sign-up" class="inline-block bg-gradient-to-r from-purple-500 to-pink-600 hover:from-purple-600 hover:to-pink-700 text-white font-bold py-3 px-6 rounded-xl transition duration-200 transform hover:scale-105 shadow-lg">
                            Join to Post
                        </a>
                    <% } %>
                </div>
            <% } else { %>
                <!-- Posts List -->
                <div class="space-y-6">
                    <% posts.forEach((post, index) => { %>
                        <div class="bg-white rounded-2xl shadow-xl overflow-hidden transform transition duration-200 hover:shadow-2xl hover:-translate-y-1">
                            
                            <!-- Post Header -->
                            <div class="bg-gradient-to-r from-purple-100 to-pink-100 px-6 py-4 border-b border-gray-200">
                                <div class="flex items-start justify-between">
                                    <div class="flex-1">
                                        <h2 class="text-2xl font-bold text-gray-800 mb-1">
                                            <%= post.title %>
                                        </h2>
                                        <div class="flex items-center gap-2 text-sm">
                                            <span class="text-gray-500">
                                                <%= new Date(post.created_at).toLocaleDateString('en-US', { 
                                                    year: 'numeric', 
                                                    month: 'long', 
                                                    day: 'numeric' 
                                                }) %>
                                            </span>
                                            <span class="text-gray-400">•</span>
                                            <span class="text-gray-500">
                                                Post #<%= posts.length - index %>
                                            </span>
                                        </div>
                                    </div>

                                    <!-- Author Badge -->
                                    <div class="flex items-center gap-2">
                                        <% if (locals.user && post.username) { %>
                                            <!-- Logged In - Show Author -->
                                            <div class="bg-gradient-to-r from-blue-500 to-indigo-600 text-white px-4 py-2 rounded-full flex items-center gap-2 shadow-md">
                                                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                                                    <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd"></path>
                                                </svg>
                                                <span class="font-bold">@<%= post.username %></span>
                                            </div>
                                        <% } else { %>
                                            <!-- Guest - Show Anonymous -->
                                            <div class="bg-gradient-to-r from-gray-400 to-gray-600 text-white px-4 py-2 rounded-full flex items-center gap-2 shadow-md">
                                                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                                                    <path fill-rule="evenodd" d="M3.707 2.293a1 1 0 00-1.414 1.414l14 14a1 1 0 001.414-1.414l-1.473-1.473A10.014 10.014 0 0019.542 10C18.268 5.943 14.478 3 10 3a9.958 9.958 0 00-4.512 1.074l-1.78-1.781zm4.261 4.26l1.514 1.515a2.003 2.003 0 012.45 2.45l1.514 1.514a4 4 0 00-5.478-5.478z" clip-rule="evenodd"></path>
                                                    <path d="M12.454 16.697L9.75 13.992a4 4 0 01-3.742-3.741L2.335 6.578A9.98 9.98 0 00.458 10c1.274 4.057 5.065 7 9.542 7 .847 0 1.669-.105 2.454-.303z"></path>
                                                </svg>
                                                <span class="font-bold">Anonymous</span>
                                            </div>
                                        <% } %>
                                    </div>
                                </div>
                            </div>

                            <!-- Post Content -->
                            <div class="px-6 py-6">
                                <p class="text-gray-700 text-lg leading-relaxed whitespace-pre-wrap"><%= post.content %></p>
                            </div>

                            <!-- Post Footer -->
                            <div class="px-6 py-4 bg-gray-50 border-t border-gray-200 flex items-center justify-between">
                                <div class="flex items-center gap-4 text-sm text-gray-500">
                                    <span class="flex items-center gap-1">
                                        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                                        </svg>
                                        <%= Math.floor(Math.random() * 100) + 20 %> views
                                    </span>
                                </div>

                                <!-- Admin Delete Button -->
                                <% if (locals.user && user.role === 'admin') { %>
                                    <form action="/delete-post/<%= post.id %>" method="GET" 
                                          onsubmit="return confirm('⚠️ Are you sure you want to delete this post? This action cannot be undone!');"
                                          class="inline">
                                        <button 
                                            type="submit"
                                            class="bg-red-500 hover:bg-red-600 text-white font-semibold px-4 py-2 rounded-lg transition duration-200 flex items-center gap-2 shadow-md hover:shadow-lg"
                                        >
                                            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                                            </svg>
                                            Delete Post
                                        </button>
                                    </form>
                                <% } %>
                            </div>

                        </div>
                    <% }); %>
                </div>
            <% } %>

            <!-- Floating Action Button (for members) -->
            <% if (locals.user) { %>
                <a href="/new-post" 
                   class="fixed bottom-8 right-8 bg-gradient-to-r from-purple-500 to-pink-600 hover:from-purple-600 hover:to-pink-700 text-white p-4 rounded-full shadow-2xl transition duration-200 transform hover:scale-110 z-50">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
                    </svg>
                </a>
            <% } %>

        </div>
    </div>

</body>
</html>