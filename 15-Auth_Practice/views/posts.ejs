<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Posts</title>
</head>
<body>
    <h1>Posts</h1>

    <% posts.forEach(post=> { %>
        <div style="display: flex; gap: 20px; justify-content: space-between; align-items: center; border: 2px solid #333333;">
            <h1>Poster: <%= post.id %></h1>
            <h3><%= post.title %></h3>
            <p><%= post.content %></p>
        </div>
    <% }) %>
</body>
</html>